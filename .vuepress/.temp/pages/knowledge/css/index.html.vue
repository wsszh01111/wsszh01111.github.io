<template><h1 id="css" tabindex="-1"><a class="header-anchor" href="#css" aria-hidden="true">#</a> CSS</h1>
<p>FE基本功（虽然调样式细节挺烦的）</p>
<p>不过CSS通过一整套描述语言控制页面样式的渲染，还是非常强啊；简单想想情况会非常复杂，以width为例，盒的宽度可能是由其本身决定（<code>width: xpx</code>）、由父宽度控制（<code>width: x%</code>）、子元素控制（<code>display: inline;</code>），想来浏览器的实际实现应该会非常复杂，也非常考验编码功力</p>
</template>
