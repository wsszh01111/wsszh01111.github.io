<template><h1 id="parseint" tabindex="-1"><a class="header-anchor" href="#parseint" aria-hidden="true">#</a> parseInt</h1>
<p>解析字符串并返回基数的十进制数字</p>
<p>解析字符串时会先trim，解析+/-， <code>parseInt </code>遇到的字符不是指定 <code>radix </code>参数中的数字，它将忽略该字符以及所有后续字符；如果第一个字符不能转换为数字，<code>parseInt</code>会返回 <code>NaN</code>。</p>
</template>
